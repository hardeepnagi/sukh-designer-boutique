document.getElementById('review-form').addEventListener('submit', function(e){e.preventDefault(); const input = document.getElementById('review-input'); if(input.value){const ul = document.getElementById('review-list'); const li = document.createElement('li'); li.textContent = input.value; ul.appendChild(li); let stored = JSON.parse(localStorage.getItem('reviews')||'[]'); stored.push(input.value); localStorage.setItem('reviews',JSON.stringify(stored)); input.value='';}});